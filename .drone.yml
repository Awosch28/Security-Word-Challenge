---
#####################
# Deploy to Staging #
#####################

kind: pipeline
type: exec
name: deploy-staging

trigger:
  event:
  - push

steps:
- name: deploy
  commands:
  - pip3 install -r requirements.txt
  - python3 webapp/app.py
  - python3 webapp/app.py

node:
  group: wordle